# CircleCI config
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.12
    working_directory: ~/challenge-bypass-extension
    steps:
      - checkout
      - run: yarn install
      - run: yarn lint
      - run: yarn build
      - run: yarn test
